# 文档解析层设计方案

## 一、设计目标

为后续 AI Agent 提供**全量、无损、可检索**的文档内容，支持：

- 多层嵌套文档（word → excel/pdf/word/zip/图片 → 更深层）
- 多种内容载体（文字段落 / 表格 / 图片 / 嵌入文件）
- 不同量级的数据（配置表 vs 业务明细大表）
- Agent 按关键词定位、按上下文检索、按层级导航

**解析层不做**：语义理解、规则匹配、报告生成。只做格式解包 + 全量提取 + 文件输出。

---

## 二、整体流程

```
输入: UR_FS.docx（多层嵌套）
    │
    ▼
┌──────────────────────────────────────────────────────┐
│                  递归提取器                           │
│                                                      │
│  Step 1: 解析本层文档                                │
│  ├── .docx → POI 提取段落/表格 → body.md            │
│  ├──     → 提取 media/ 图片 → 存原文件              │
│  ├──     → 发现嵌入文件 → 记录到 manifest            │
│  ├── .xlsx → POI 提取各 Sheet                       │
│  │   ├── 小表(≤50行) → 直接写入 body.md              │
│  │   └── 大表(>50行) → 写入 data/ 独立 csv          │
│  ├── .pdf  → PDFBox 提取文本 → body.md               │
│  └── .zip  → 解压 → 递归提取                        │
│                                                      │
│  Step 2: 对嵌入文件递归执行 Step 1                   │
│    (depth+1，记录 parent_level + parent_position)     │
│                                                      │
│  Step 3: 更新 manifest                               │
└──────────────────────────────────────────────────────┘
    │
    ▼
输出目录结构
```

---

## 三、输出目录结构

```
extracted/{session_id}/
│
├── manifest.json            ← 层级关系索引（JSON，小文件）
│   {
│     "source_file": "UR_FS.docx",
│     "levels": [
│       {"level":0, "dir":"level_0", "source":"UR_FS.docx", "type":"docx",
│        "element_count":12},
│       {"level":1, "dir":"level_1", "source":"subsidy.xlsx", "type":"xlsx",
│        "parent_level":0, "parent_position":2, "element_count":5}
│     ]
│   }
│
├── level_0/                  ← 第0层：根文档
│   ├── body.md              ← 按文档顺序，一行一条记录
│   │   ┌────────────────────────────────────────────────┐
│   │   │ # DOC: 0                                        │
│   │   │ # SOURCE: UR_FS.docx                            │
│   │   │ # POS: 0 | TYPE: paragraph | 用户信息录入页面    │
│   │   │ # POS: 1 | TYPE: table                          │
│   │   │ | 字段 | 类型 | 是否敏感 |                      │
│   │   │ | 姓名 | string | 是 |                          │
│   │   │ | 年龄 | int | 否 |                             │
│   │   │ # POS: 2 | TYPE: embed | file: subsidy.xlsx |   │
│   │   │   ref_level: 1                                  │
│   │   │ # POS: 3 | TYPE: image | file: media/img1.png  │
│   │   │ # POS: 4 | TYPE: paragraph | 数据报送要求如下   │
│   │   └────────────────────────────────────────────────┘
│   │
│   ├── media/               ← 图片文件（原样）
│   │   ├── image1.png
│   │   └── image2.jpeg
│   │
│   └── assets/              ← 其他原始附件
│       └── ...
│
├── level_1/                  ← 第1层：嵌入的 Excel
│   ├── body.md
│   │   ┌────────────────────────────────────────────────┐
│   │   │ # DOC: 1                                        │
│   │   │ # SOURCE: subsidy.xlsx                          │
│   │   │ # PARENT: level_0, pos=2                        │
│   │   │ # POS: 0 | TYPE: sheet_header | [Sheet: 字段定义]│
│   │   │ # POS: 1 | TYPE: table                          │
│   │   │ | 字段名 | 类型 | 长度 |                        │
│   │   │ | 客户号 | string | 20 |                        │
│   │   │ | 身份证号 | string | 18 |                      │
│   │   │ # POS: 2 | TYPE: sheet_header | [Sheet: 数据明细]│
│   │   │ # POS: 3 | TYPE: data_ref |                     │
│   │   │   请参考: data/数据明细.csv（共2000行）          │
│   │   │   schema: 序号,客户名,金额,日期                 │
│   │   │   preview: 1,张三,500.00,2024-01-01             │
│   │   └────────────────────────────────────────────────┘
│   │
│   ├── data/                ← 大表的独立文件
│   │   └── 数据明细.csv
│   │
│   └── media/
│
├── level_2/                  ← 第2层：嵌入 Excel 里的 Word
│   ├── body.md
│   └── ...
│
└── level_3/
    └── ...
```

---

## 四、body.md 格式规范

### 4.1 文件头

```
# DOC: {level}
# SOURCE: {文件名}
# PARENT: {父级目录}, pos={父级position}    ← 非根文档才有
```

### 4.2 条目行（一行一条，等号分隔）

```
# POS: {position} | TYPE: {类型} | {元数据}
{正文内容}
```

### 4.3 类型定义

| TYPE | 含义 | 元数据 | 正文内容 |
|------|------|--------|---------|
| `paragraph` | 文字段落 | - | 段落原文 |
| `table` | 表格（小表 ≤50行） | - | Markdown 表格格式 |
| `table_large` | 大表引用 | `schema:...` `rows:2000` `file:data/xxx.csv` | 前3行预览 |
| `embed` | 嵌入文件 | `file:xxx.xlsx` `ref_level:N` | - |
| `image` | 图片 | `file:media/xxx.png` | - |
| `sheet_header` | Excel Sheet 名 | - | Sheet 名称 |

### 4.4 示例

```
# DOC: 0
# SOURCE: UR_FS.docx
# POS: 0 | TYPE: paragraph
用户信息录入页面需要收集以下个人信息
# POS: 1 | TYPE: table
| 字段 | 类型 | 是否敏感 |
| 姓名 | string | 是 |
| 年龄 | int | 否 |
# POS: 2 | TYPE: embed | file: subsidy.xlsx | ref_level: 1
# POS: 3 | TYPE: image | file: media/page_prototype.png
# POS: 4 | TYPE: paragraph
以上字段需进行脱敏处理
```

---

## 五、不同内容的处理策略

### 5.1 Excel 表格 — 按量级分流

| 特征 | 判断条件 | 存储方式 |
|------|---------|---------|
| 配置表 / 元数据 | 行数 ≤ 50 且 列数 ≤ 10 | 全量转 Markdown 表格写入 body.md |
| 业务明细表 | 行数 > 50 或 列数 > 10 | body.md 中写 `data_ref` + schema + 前3行预览；全量数据写 `data/{sheet}.csv` |

### 5.2 图片 — 原样保留，不做 OCR

- 保留原始文件到 `media/`
- 在 body.md 中标记位置
- **后续 AI 层**按需调用 Qwen-VL

### 5.3 嵌入 Word — 递归展开

- 解包嵌入的 .docx
- 提取段落/表格/图片
- 写入新的 level body.md
- 如果还有嵌入文件，继续递归

### 5.4 嵌入 PDF — 文本提取

- PDFBox 提取文本
- 按段落写入 body.md
- PDF 中的图片暂不提取（终端节点）

### 5.5 嵌入 ZIP — 递归解压

- 解压 ZIP
- 每个文件按格式进入对应处理流程
- 嵌套深度限制：默认 10 层

---

## 六、Agent 使用方式

### 6.1 检索入口

Agent 不需要解析 JSON，直接读 body.md 即可：

```
# 全文搜索包含"证件号码"的段落
grep "证件号码" extracted/{session_id}/level_*/body.md

# 按阅读顺序浏览全部内容（含嵌套文件）
cat extracted/{session_id}/level_*/body.md

# 只查看某层内容
cat extracted/{session_id}/level_0/body.md

# 查看大表数据
head -10 extracted/{session_id}/level_1/data/数据明细.csv
```

### 6.2 位置导航

通过 manifest.json 获知层级关系后，Agent 可以：

```
# 知道某嵌入文件对应哪个 level
→ 直接读对应 level 的 body.md

# 知道某条内容在文档中的位置
→ 看 POS 编号 + PARENT 关系

# 需要上下文
→ 取同一文件的 POS 前后行 + PARENT 链上的内容
```

---

## 七、主要涉及代码模块

| 模块 | 职责 |
|------|------|
| `RecursiveExtractor` | 入口，协调整个提取流程 |
| `DocxHandler` | 处理 .docx：提取段落/表格/图片/嵌入 |
| `ExcelHandler` | 处理 .xlsx：分析大小，小表转 md，大表写 csv |
| `PdfHandler` | 处理 .pdf：PDFBox 提取文本 |
| `ZipHandler` | 处理 .zip：解压后递归 |
| `PositionTracker` | 维护层级 + position 编码 |
| `StoreWriter` | 写入 body.md + manifest.json + data/ |

---

## 八、与旧方案的区别

| 维度 | 旧方案（纯内存） | 新方案（落盘目录） |
|------|---------------|----------------|
| 输出 | Java 对象树 | 目录 + 文本文件 |
| 嵌套文件 | 递归解析，返回嵌套对象 | 展开为独立 level 目录 |
| 大表数据 | 全量塞入 JSON | 分到 data/csv |
| 图片 | 存 byte[] 在对象里 | 存原始文件到 media/ |
| 检索入口 | 需遍历对象树 | grep body.md + manifest |
| 缓存 | 不支持 | 目录结构可缓存，按需重新提取 |
| 数据格式 | JSON | **纯文本 md 为主**，少量元数据用 JSON |

---

## 九、后续 AI 层接入点

解析层输出产物 → AI 层消费方式：

```
body.md                → 全文搜索 / 按 POS 定位 / 取上下文
manifest.json          → 了解文档骨架和层级关系
media/*.png           → Qwen-VL 多模态分析
data/*.csv            → 按需读取（只取 Agent 需要的行/列）
```
